name: Deploy Docker Image

on:
  pull_request:
    branches: [main]
    types: [closed]
  workflow_dispatch:
    inputs:
      no-cache:
        description: Build Docker image without using cache
        required: false
        default: false
        type: boolean

jobs:
  publish:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true
    uses: hyuabot-developers/hyuabot-infrastructure/.github/workflows/publish-immutable-image.yml@8f1f01759d39340962b33d8cbba4835980715a4c
    with:
      image-repository: localhost:5000/hyuabot-library-updater
      workload-kind: cronjob
      workload-name: reading-room-cron-job
      container-name: reading-room-cron-job
      build-secret-path: google-service-account.json
      no-cache: ${{ github.event_name == 'workflow_dispatch' && inputs['no-cache'] }}
    secrets:
      RUNNER_ADMIN_TOKEN: ${{ secrets.RUNNER_ADMIN_TOKEN }}
      FIRST_HOST: ${{ secrets.SSH_HOST || secrets.ORACLE_TRANSFER_TARGET }}
      BUILD_SECRET_FILE: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
